
class RootMeaning {
  constructor(meaningId, rootId, meaning) {
    this.meaningId = meaningId;
    this.rootId = rootId;
    this.meaning = meaning;
  }

  // Define any model methods here
}

module.exports = RootMeaning;
