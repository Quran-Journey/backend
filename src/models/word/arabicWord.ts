
class ArabicWord {
  private wordId: number;
  private word: string;
  private rootId: number;
  constructor(wordId:number, word:string, rootId:number) {
    this.wordId = wordId;
    this.word = word;
    this.rootId = rootId;
  }

  // Define any model methods here
}

export default ArabicWord;
