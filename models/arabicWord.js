
class ArabicWord {
  constructor(wordId, word, rootId) {
    this.wordId = wordId;
    this.word = word;
    this.rootId = rootId;
  }

  // Define your model methods here
}

module.exports = ArabicWord;
